namespace MVCIntro.Models
{
    public class Login
    {
        //Only thing that needs to be in here is C# properties'
        //Name attributes in the form must match the names of our properties, spelling/casing must be the exact same 
       public string Username { get; set; }
       public string Password { get; set; }
       public int Age { get; set; }
    }
}
